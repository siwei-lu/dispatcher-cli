{
  "name": "dispatcher-cli",
  "version": "0.6.0",
  "description": "Unified non-interactive entry point for AI coding agents (claude -p, codex exec)",
  "type": "module",
  "bin": {
    "dispatch": "./src/index.ts"
  },
  "files": [
    "src",
    "docs",
    "README.md"
  ],
  "scripts": {
    "dev": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "build:bin": "bun build src/index.ts --compile --outfile dist/dispatch",
    "prepare": "husky"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "dependencies": {
    "cac": "^6.7.14"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "husky": "^9.1.7",
    "prettier": "^3.3.0",
    "typescript": "^5.6.0"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true,
    "trailingComma": "all",
    "printWidth": 80,
    "tabWidth": 2,
    "arrowParens": "always"
  }
}
